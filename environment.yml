name: holspec
channels:
  - conda-forge
dependencies:
  - python=3.12.*
  - pip

  # Core package dependencies
  - numpy
  - scipy
  - pandas
  - gudhi
  - h5py
  - natsort
  - nbformat
  - pyyaml

  # Visualization and examples
  - matplotlib
  - pillow
  - seaborn

  # Notebook workflows
  - jupyterlab
  - ipykernel
  - ipywidgets
  - ipympl

  # Data ingestion and downstream analysis
  - pyarrow
  - openpyxl
  - pytables
  - scikit-learn

  # CLI and workflow utilities
  - rich
  - tqdm
  - typer

  # Development tools
  - pytest
  - ruff

  # Pip packages, if needed
  - pip: []
